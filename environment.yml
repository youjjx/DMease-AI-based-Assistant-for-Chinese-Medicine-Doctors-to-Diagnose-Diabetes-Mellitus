name: dmease
channels:
  - pytorch
  - conda-forge
  - defaults
dependencies:
  - python=3.10
  - pip
  - numpy
  - pandas
  - networkx
  - pyyaml
  - scikit-learn
  - pytorch
  - pip:
      - streamlit
      - pytest
      - ruff

